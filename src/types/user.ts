export interface User {
    id: string;
    email: string;
    name: string;
    rootDirId: number;
}

export interface UserDto {
    id: string;
    email: string;
    name: string;
    root_dir_id: number;
}
