export interface User {
    id: string;
    login: string;
    name: string;
    rootDirId: number;
}

export interface UserDto {
    id: string;
    login: string;
    name: string;
    root_dir_id: number;
}
